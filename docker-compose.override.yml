version: '3.4'

services:
  scraper:
    ports:
      - 3000:3000
    volumes:
      - ./scraper:/usr/src/app
      - /usr/src/app/node_modules

  monitor:
    image: scraper
    ports:
      - 4567:4567
    command: 'npm run monitor'
    depends_on:
      - scraper
      - postgres
      - redis

  test:
    image: scraper
    depends_on:
      - runner
      - scraper
    volumes:
      - ./scraper:/usr/src/app
      - /usr/src/app/node_modules

  frontend:
    ports:
      - 8080:8080
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules
