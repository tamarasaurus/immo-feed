import * as React from 'react';
import { RouteComponentProps } from '@reach/router';

export default class Results extends React.Component<RouteComponentProps> {
    render() {
        return <div>
            Results
        </div>
    }
}