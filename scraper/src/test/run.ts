describe('it runs the scraper', () => {
    it('runs the scraper at a configured interval')
    it('gets results from each source')
    it('stores results from each source')
    it('triggers a notification for new results')
})
